export class Member {
  constructor(public nick: string,
              public auto: string,
              public city: string,
              public number: string,
              public id?: number) {
  }
}
